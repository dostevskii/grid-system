# 검증 기록

Windows / Node.js 22.15.0 / Chrome headless.

## 1.2.0 로컬 결과 — 2026-09-17

- `npm test`: 4개 파일, 46개 테스트 통과. 단계별 시드·잠금·기존 JSON 마이그레이션, 잘못된 잠금/비율 상태, 빈 그리드 단계, pt 단위 양자화, 본문 줄 길이, 제목 행간 비율, 제목과 본문 사이의 온전한 단어 흐름을 확인했다.
- `npm run build`: TypeScript 검사와 Vite 프로덕션 빌드 통과.
- Playwright: 19개 통과. 단계별 생성, 시드 재현과 그리드 불변성, 잠금에 따른 수동 편집 차단, JSON 재불러오기·새로고침, 320/390px 조작부, 레거시 seed 314·1px 행간·pt 입력을 확인했다.
- 오프라인 ZIP을 실제 브라우저에서 열어 이미지 박스의 SVG·HTML 좌표, 인쇄 좌표, 390px 화면의 종횡비와 넘침 방지, 그리드 전용 보기의 박스 제외를 검증했다.
- 실제 웹폰트로 4,800개 구성을 점검했다: 12종 서체 × 50개 시드 × 기본 4×5/랜덤 데스크톱/390×844/A4 × 타이포/타이포+이미지. 제목·본문 누락, 세 줄 미만 본문, 자동 행간 겹침 경고, 텍스트 경계 이탈, 이미지 비율·예약 영역 이탈, 평균 90자 초과 본문 줄은 없었다. 독일어 단어 줄바꿈 때문에 10개 본문에서 평균 18~19자의 짧은 줄이 관찰됐다.
- 읽기 조건은 실제 선택한 폰트와 원문을 측정해 적용한다. 일반 본문 후보 폭은 평균 문자 폭의 27~78배이며, 한 모듈 자체가 더 넓은 수동 그리드에서는 임의로 셀을 쪼개지 않는다. 일반 랜덤 행간은 글자 크기의 약 1.42~1.62배이고 수동 입력은 1px까지 그대로 허용한다.
- 32×31 수동 그리드의 타이포+이미지 계산은 점검 환경에서 약 23ms였으며 본문과 박스가 생성되고 경고가 없었다. 기기별 성능을 보장하는 수치는 아니다.
- 320, 390, 600, 768, 1024, 1200, 1440px 폭에서 조작부 좌표·넘침을 확인했다. README의 실제 화면 3장을 갱신했다.
- frontend-design과 impeccable 지침은 기존 편집기 스타일을 유지하며 두 단계의 작업 순서와 되돌릴 수 있는 잠금 상태를 명확하게 만드는 데 적용했다. 디자인 검사기의 Inter 경고는 사용자 지정 기본 서체이자 기존 UI의 일부이므로 변경하지 않았다.

독립적인 화면 검토 결과:

| 검토 항목 | 판정 | 남은 필수 수정 |
| --- | --- | --- |
| 단계별 작업 흐름 | PASS | 없음 |
| 기존 시각 체계 유지 | PASS | 없음 |
| 타이포·이미지 박스·내보내기 | PASS | 없음 |
| 데스크톱·모바일 구성 | PASS | 없음 |
| 요청 범위와 마감 품질 | PASS | 없음 |

## 1.1.1 로컬 결과

- `npm test`: 4개 파일, 32개 테스트 통과. 폰트 크기 범위 안내가 영어·한국어 모두 pt로 표시되는지 추가 확인했다.
- `npm run build`: TypeScript 검사 및 Vite 프로덕션 빌드 통과.
- Playwright: 전체 13개 통과(18.5초). pt 전용 입력과 단위 선택기 제거, 기본 12pt, 18pt 입력의 SVG·HTML·JSON 24px 출력, 랜덤 배치의 pt 환산, 이전 JSON의 22px → 16.5pt 표시, 한국어 UI·초기화·새로고침·범위 오류를 확인했다.
- 행간·자간의 px 입력과 1px 행간 겹침은 유지된다. 기존 JSON의 실제 크기와 랜덤 시드 결과는 바뀌지 않는다.
- README 이미지 3장을 다시 캡처하고, 1.1.1 편집 화면의 12pt 표시와 px/pt 선택기 제거를 시각 확인했다.

## 1.1.0 로컬 결과

- `npm test`: 4개 파일, 31개 테스트 통과. 기존 산식·원문·프리셋·내보내기에 시드 재현, 모든 랜덤 필드의 변화, 등록 서체별 지원 굵기, 1×1/1×N/N×1 자유 배치, 전체 10,000행 제한, 영문 오류·프리셋 번역을 추가했다.
- `npm run build`: TypeScript 검사 및 Vite 프로덕션 빌드 통과.
- Playwright: 전체 11개 통과. 기존 8개를 유지하고 영어 기본값·한국어 선택 저장, JSON 전체 값으로 확인한 시드 재현·새 시드·새로고침 복원, 자유 배치, 1px 행간의 SVG/HTML 기준선 간격 검사를 추가했다.
- 고정 4×5 free 배치 500개 시드에서 제목과 본문의 시작 셀이 각각 20칸 전체를 포함했다. `seed: 314`는 1-based 행 우선 번호 기준 제목 9번 칸과 본문 11번 칸을 함께 사용한다.
- 변수 선택용 난수 스트림과 배치용 스트림을 분리해 열·행 수가 제목의 오른쪽·아래쪽 편향으로 연결되지 않도록 했다.
- 겹치는 행간을 미리보기에서 숨기지 않으며 SVG·HTML/CSS·JSON에 보존한다. 수치 변경 직후에는 현재 설정의 배치 계산이 끝나기 전까지 내보내기를 막는다.
- 실제 Chrome에서 320, 390, 600, 768, 1024, 1200, 1440px 폭의 상단 랜덤·시드·언어 조작부가 화면 안에 있는지 확인했다. 모바일·중간 폭·데스크톱 스크린샷과 여러 A4 자유 배치를 시각 검토했다.
- README 이미지 3장을 1.1.0 실제 앱에서 다시 캡처했다. 인쇄 예시는 A4 / Libre Baskerville / seed 149이며, 로딩 완료 상태를 확인하고 캡처한다.

## 1.0.0 로컬 결과

- `npm test`: 3개 파일, 20개 테스트 통과. 산식, 중첩 입력 검증, JSON 정규화, 원문 흐름, 긴 단어, 배치 변형과 경계, ISO A/B·JIS B·국내 치수, 내보내기를 검사했다.
- `npm run build`: TypeScript 검사 및 Vite 프로덕션 빌드 통과.
- `npm run test:e2e`: 8개 브라우저 테스트 통과. 12종 폰트와 20/32분할, 잘못된 입력/JSON, 폰트 실패·재시도, 모바일 패널·다이얼로그, A4 실치수, SVG/JSON/ZIP 다운로드를 확인했다.
- ZIP에 담긴 HTML/CSS와 폰트를 외부 네트워크 없이 로드했다. Inter·Libre Baskerville·열린명조의 SVG와 HTML 줄 위치·기준선·폭, 그리드 셀과 불투명도를 비교했다.
- 390px 모바일 HTML의 긴 독일어 단어 줄바꿈과 인쇄 모드의 A4 물리적 크기를 확인했다.
- 기본 데스크톱·모바일 및 SVG/HTML 비교 스크린샷을 검토했다. 스크린샷과 추적 로그는 Git에서 제외했다.
- 사용자 원문 파일과 `src/sample.txt`의 내용이 줄바꿈 방식·마지막 개행을 제외하고 일치함을 확인했다.
- 설치 시 `npm audit`: 취약점 0개.

## 폰트

82개 WOFF2와 SHA-256 목록을 보존한다. 10종은 검사한 독일어 문자 `äöüÄÖÜß«»`를 포함한다. 열린명조·열린고딕의 움라우트 6자는 Inter로 보완하며 같은 fallback을 측정·미리보기·SVG·HTML에 적용한다. OFL 10종과 열린 서체의 별도 출처 고지를 구분한다.

## 범위와 한계

- 새 랜덤 생성은 그리드·콘텐츠를 분리하고 읽기 조건을 적용한 시드 배치를 제공한다. 임의 드래그 편집기는 아니며, 실제 디자인에 사용할 문장·이미지를 넣은 후 최종 검토가 필요하다. 매우 작거나 극단적인 수동 그리드에서 조건을 만족하지 못하면 조정 안내를 표시한다. 기존 JSON은 이전 정형/자유 배치를 유지한다.
- 작은 행간의 줄 겹침은 의도적으로 허용하지만 블록의 그리드 영역끼리는 겹치지 않는다. 과도한 텍스트 생성에는 전체 10,000행 상한과 안내가 적용된다.
- SVG는 편집 가능한 텍스트를 유지하므로 Figma 등의 가져오기 환경에 같은 폰트가 필요하다. Figma 네이티브 Layout Guide 생성·SVG 글자 윤곽선화는 포함하지 않는다.
- 인쇄 크기는 mm/inch로 보존하지만 색분해, CMYK, 도련, 재단선 등 전문 인쇄 프리플라이트는 포함하지 않는다.
- 원래 샘플의 미완성 마지막 구절도 보존한다.
- 폰트 출처와 이용 근거의 상세 내용은 `public/font-notices/`에 포함한다.

## 1.1.0 배포

- 앱 코드 커밋 `ad4aa3038cb23bece1a423a869bdd61b30a856b7`을 비공개 GitHub `main`에 push했다. GitHub Actions 빌드·단위 테스트가 성공했다.
- Cloudflare Git 자동 배포 `6b62d95f-2217-434c-b12e-220645181dcb`의 production `deploy/success`를 확인했다.
- https://grid-system.pages.dev/ 에서 전체 브라우저 테스트 **11/11 통과 (17.5초)**. 영어/한국어, 시드 JSON 재현·복원, 새 시드, 자유 배치, 1px 겹침 출력과 기존 기능을 확인했다.
- 공개 주소의 HTTP 200, 영어 기본 HTML, 로컬 빌드와 같은 JavaScript asset `/assets/index-DPuYzvon.js`, `X-Frame-Options: DENY`를 확인했다.
- 저장소의 Private 상태와 About의 상세 설명·홈페이지 주소를 확인했다. 인증 정보와 참고 PDF는 추가하지 않았다.

## 1.0.0 초기 배포

비공개 GitHub 저장소 https://github.com/dostevskii/grid-system 의 Private 상태와 `main` push를 확인했다. 구현 커밋 `8813402`의 GitHub Actions 빌드·단위 테스트도 성공했다.

사용자 승인 후 Cloudflare GitHub App을 `grid-system` 저장소에 한정해 연결했다. 최초 `8000011` Git installation 오류와 연결 복귀 문제는 앱 재연결 후 해소됐으며, Git-integrated Pages 프로젝트 `grid-system`을 환경 변수 인증으로 생성했다. Direct Upload로 전환하지 않았고 토큰을 저장소나 GitHub Actions에 복사하지 않았다.

- 공개 주소: https://grid-system.pages.dev/
- 첫 배포: `02a5e075-25b8-4183-9f27-9c0044ae79f1`, `production`, 최종 `deploy/success`.
- 검증 대상 커밋: `7b0aee7556d58672675bd0738ec1a4e0a216dd28`.
- 배포 설정: GitHub `dostevskii/grid-system`, `main`, `npm run build`, `dist`, Node.js `22.15.0`. `main` 자동 배포 활성화, 다른 브랜치 preview와 PR 댓글 비활성화.
- 공개 주소에서 `GRID_SYSTEM_BASE_URL=https://grid-system.pages.dev`로 브라우저 테스트 8개 전부 통과(25.7초). 12종 폰트, 모바일, 입력·폰트 실패 복구, 세 형식 다운로드와 오프라인 출력, SVG/HTML 배치 일치를 확인했다.
- HTTPS 응답 `200`, CSP와 `X-Frame-Options: DENY` 적용을 확인했다. 실제 Chrome에서 초기 20분할·Inter 문단과 로딩 완료 상태를 시각적으로 확인했다.
- 배포 후 GitHub 저장소의 `isPrivate: true`를 재확인했다.
- 배포 스크립트의 PowerShell 구문 검사와 읽기 전용 상태 조회가 통과했다. `-Deploy`의 정상 요청과 다른 저장소 차단도 네트워크 없는 mock 검사로 확인했다. `-Deploy`는 검증된 Git 저장소·production branch의 첫 배포/재배포 요청을 지원한다.
