# Grid System 제작 계획

작성일: 2026-09-16  
상태: 2026-09-16 전체 계획 승인 후 구현·로컬 검증·비공개 GitHub push·Cloudflare Pages 배포를 마쳤다. 공개 주소 https://grid-system.pages.dev/ 에서 브라우저 테스트 8개를 통과했다. 실제 검증 결과와 한계는 README.md와 VERIFICATION.md에 기록한다.  
이름: Grid System  
프로젝트 위치: `C:\Users\JohnHB\Documents\Codex\2026-09-16\grid-system`

## 0. 제품 이름

### 1.1.0 확장

사용자의 후속 요청에 따라 최소 1px 행간의 의도적인 겹침, 상단 시드 기반 전체 변수 랜덤 생성, 제목·본문의 전 셀 자유 배치, English 기본/한국어 전환을 추가한다. 기존 세 가지 정형 배치는 유지하고 `free` 배치를 추가한다. 랜덤 시드는 같은 작업판과 서체에서 재현되며, 직접 수정한 최종 수치는 설정 JSON으로 보존한다. 세부 변경은 [CHANGELOG.md](CHANGELOG.md)에 기록한다.

사용자가 최종 지정한 제품명은 **Grid System**이다. 앱의 로고 텍스트, 브라우저 제목, 소개, README에서 같은 이름을 사용한다.

프로젝트 폴더와 저장소 slug는 `grid-system`을 사용한다. GitHub 저장소와 Cloudflare Pages 프로젝트 이름의 사용 가능 여부는 생성 단계에서 확인한다.

## 1. 목적과 해석

디자이너가 그리드 수치와 타이포그래피를 조절하면서, 실제 문단이 배치된 결과를 보고 재사용 가능한 레이아웃을 내려받는 웹 도구를 만든다. Müller-Brockmann의 명료함, 질서, 비례, 일관된 정렬과 정보의 위계라는 원칙을 현대 웹 작업에 적용한다.

책의 도판을 그대로 복제하는 것이 아니라, 모듈의 결합과 여백, 제목·본문의 크기 차이로 여러 구성이 성립하는 과정을 인터랙션으로 보여준다. 앱의 짧은 소개 패널에서 이 해석과 참고 도서를 밝힌다. 책에서 정해진 규칙과 이 제품이 제안하는 기본값을 구분한다.

제공된 PDF의 관련 페이지를 이미지로 확인했다. 페이지 번호는 PDF 뷰어 번호가 아니라 책에 인쇄된 번호다.

- 10–12쪽: 질서와 명료함을 위한 그리드, 모듈과 텍스트의 관계.
- 58–59쪽: 모듈 높이와 온전한 행의 수, 제목·본문·캡션의 행간 관계.
- 76, 78–79쪽: 20분할의 구성과 다양한 크기의 모듈 결합.
- 87, 89–90쪽: 32분할과 4열 × 8행의 구성.

## 2. 첫 버전의 기능

| 영역 | 포함할 기능 |
| --- | --- |
| 작업판 | 인쇄/웹 프리셋 분류, 폭·높이 직접 입력, 세로·가로 방향, 화면에 맞춤 및 배율 조절 |
| 인쇄 규격 | ISO A0–A10, ISO B0–B10, JIS B0–B10, 국내 국전지·국절·완성 판형, 미국 Letter·Legal·Tabloid 등 |
| 웹 규격 | Figma의 Frame 프리셋에서 Phone·Tablet·Desktop 대표 규격을 선정하고 원래 명칭과 치수를 사용. 기본 Desktop 1440 × 1024 CSS px |
| 단위 | 웹은 px, 인쇄는 mm 기준과 px 전환. 미국 규격은 inch도 병기. 글자 크기는 px/pt 전환 |
| 그리드 | Columns, Rows, 상·우·하·좌 Margin, 가로·세로 Gutter 입력. 여백과 간격은 연결/개별 입력 |
| 분할 | `Columns × Rows`를 실시간 계산. 모듈 폭·높이와 유효 작업 영역 표시 |
| 프리셋 | 기본 20분할 = 4열 × 5행. 32분할 = 4열 × 8행. 수동 수정 시 Custom 표시 |
| 타이포그래피 | Inter 기본값, 사용자 지정 12종 폰트 선택, 서체별 실제 지원 굵기, 글자 크기, 자간·행간 슬라이더와 직접 수치 입력 |
| 색상 | 그리드 색상·불투명도와 텍스트 색상·불투명도를 각각 조절 |
| 배치 | 정렬형, 비대칭형, 제목 강조형의 세 가지 자동 배치. 문단 양 조절 및 같은 조건에서 다른 구성 제안 |
| 보기 | 그리드만 / 텍스트만 / 그리드+텍스트. 행간 기준 베이스라인 가이드 선택 표시 |
| 저장 | 브라우저 내 마지막 설정 자동 복원, 초기화, 설정 JSON 저장·불러오기 |
| 다운로드 | 사용자 선택으로 확정: Figma용 SVG, 웹 개발용 HTML/CSS 묶음, 재편집용 설정 JSON을 모두 첫 버전에 포함 |
| 안내 | 각 수치의 의미와 짧은 그리드 원리 안내, 입력 오류를 해당 항목 옆에 표시 |

정확한 규격 목록·치수·단위 정책은 [PRESETS.md](./PRESETS.md)에 정의한다. 인쇄 크기는 ISO/JIS/국내 관행 규격을 식별해 표시한다. 웹 프리셋은 사용자 요청에 따라 Figma Frame 프리셋의 명칭과 치수를 기준으로 한다. Desktop 기본값 1440 × 1024 px는 사용자가 지정했다.

추가 폰트는 사용자 제공 `C:\Users\JohnHB\Downloads\추가폰트지원.txt`에 따라 첫 버전의 선택 목록에 포함한다. 기존 Inter를 포함한 총 12종이며, 분류와 배포 출처·로딩·재배치 정책은 [FONTS.md](./FONTS.md)에 정의한다.

샘플 텍스트는 사용자가 지정한 아래 파일에서 가져온다. 문단 순서대로 소비하고, 필요한 분량이 더 크면 처음부터 반복한다. 번역하거나 다른 문장을 생성하지 않는다. 제목 또한 원문의 구절에서 가져온다.

`C:\Users\JohnHB\Documents\Codex\2026-09-16\referenced-chatgpt-conversation-this-is-an\outputs\internationale_typographie_auszug_de_without_markers.txt`

확인된 원문은 3개 문단이며 끝에 미완성 구절 `Eine Werb`가 있다. 첫 버전에서는 제공된 텍스트를 그대로 보존한다. 원문을 교정·보완하는 작업은 포함하지 않는다.

## 3. 계산과 배치의 원칙

### 그리드 계산

```text
분할 수 = columns × rows
모듈 폭 = (작업판 폭 - 왼쪽 여백 - 오른쪽 여백 - (columns - 1) × 가로 gutter) / columns
모듈 높이 = (작업판 높이 - 위쪽 여백 - 아래쪽 여백 - (rows - 1) × 세로 gutter) / rows
여러 열을 차지한 블록 폭 = 모듈 폭 × 열 수 + 가로 gutter × (열 수 - 1)
```

내부 계산에는 소수점 정밀도를 유지하고 표시 수치만 보기 좋게 정리한다. 음수 여백, 정수가 아닌 열·행 수, 0 이하의 모듈 크기를 만드는 입력은 유효하지 않다고 설명한다. 잘못된 값으로 이미 배치된 결과를 망가뜨리거나 내보내지 않는다. 큰 입력으로 브라우저가 멈추지 않도록 작업판 크기와 모듈 수에 상한을 둔다.

### 자동 텍스트 배치

1. 선택한 구성에 따라 제목과 본문 영역을 하나 이상의 그리드 모듈에 배정한다. 빈 모듈도 구성을 만드는 요소로 사용한다.
2. 선택한 폰트와 굵기가 로드된 뒤 실제 글자 폭, 폰트 크기, 자간, 행간, 블록 폭을 바탕으로 줄바꿈과 들어갈 행의 수를 계산한다. 폰트 변경 시 같은 구성 안에서 문단을 다시 배치한다.
3. 입력한 텍스트를 읽는 순서대로 채우고, 원문 끝에 도달하면 반복한다. 경계에서 글자나 행을 반쯤 잘라 넣지 않는다.
4. 수치가 바뀌면 같은 구성을 우선 유지하면서 재배치한다. 구성 변경은 사용자가 별도로 요청했을 때 수행하며, 설정에 구성 식별자/seed를 저장한다.
5. 본문이 들어갈 수 없을 만큼 좁거나 낮은 영역에는 가능한 모듈 결합을 적용한다. 그래도 불가능하면 이유를 알리고 유효한 배치까지만 보여준다.

독일어의 긴 단어가 블록을 넘치지 않게 줄바꿈을 처리하고, 블록 사이의 충돌·텍스트 넘침을 검사한다. 모든 입력이 임의의 행간에 정확히 나누어떨어지지는 않으므로, 베이스라인 정렬 시 발생하는 잔여 공간을 계산하고 수치를 임의로 바꾸지 않는다. 정렬 가능한 시작 위치와 온전한 행만 사용한다.

### 화면 크기와 반응형 결과

- 작업 화면의 확대·축소는 실제 px 수치를 바꾸지 않는다.
- 작업판 폭·높이를 수정하면 실제 레이아웃을 다시 계산한다.
- 프리셋 변경은 작업판 규격과 방향에 적용하고, 사용자가 정한 그리드·타이포그래피 값은 유지한다. 작은 규격에서 기존 여백·간격으로 배치가 불가능해지면 오류와 조정 제안을 보여준다.
- 인쇄 프리셋은 원래의 mm 또는 inch 치수를 저장한다. 미리보기 계산은 CSS의 `1in = 96px = 25.4mm` 관계를 사용하고, 화면 배율과 인쇄 실치수를 구분한다.
- 인쇄용 SVG에는 물리적 폭·높이와 대응하는 viewBox를 넣고, HTML/CSS에는 해당 용지 크기의 인쇄 스타일을 제공한다.
- 모바일에서도 설정 패널에 접근할 수 있도록 접히는 패널을 사용한다.
- SVG는 지정한 작업판의 정확한 구성을 담는다.
- HTML/CSS는 지정한 크기의 구성을 기본으로 하며, 좁은 화면에서는 제목과 문단을 읽기 순서대로 쌓는 반응형 규칙을 제공한다. 모바일에서 데스크톱의 모듈 분할을 그대로 유지한다고 오해하지 않도록 설명한다.

## 4. 화면과 디자인 방향

작업판을 넓게 확보하고 오른쪽에 Figma의 속성 패널과 유사한 숫자 입력 영역을 배치한다. 별도의 마케팅 첫 화면 없이 앱을 열면 기본 20분할과 문단 배치가 바로 보인다.

```text
┌────────────────────────────────────────────────────────────────┐
│ Grid System  [20분할] [32분할] [Custom]           [다운로드]    │
├───────────────────────────────────────┬────────────────────────┤
│                                       │ [웹 / 인쇄] [규격]     │
│       실제 텍스트가 놓인 작업판       │ 폭 / 높이 / 방향 / 단위│
│                                       │ Columns / Rows         │
│       제목 · 본문 · 여백               │ Margin / Gutter        │
│       선택 가능한 그리드 가이드       │                        │
│                                       │ 타이포그래피           │
│                                       │ 폰트 / 크기 / 굵기     │
│                                       │ 자간 / 행간            │
│                                       │                        │
│                                       │ 색상 / 불투명도 / 배치 │
├───────────────────────────────────────┴────────────────────────┤
│ 4 × 5 = 20분할   모듈 크기       [그리드] [베이스라인]  [배율]│
└────────────────────────────────────────────────────────────────┘
```

입력 패널을 왼쪽에 두는 대안도 가능하지만, 오른쪽 속성 패널이 요청한 Figma식 조작과 잘 맞고 작업판을 먼저 읽게 하므로 이 구성을 제안한다. 데스크톱 기준 속성 패널은 약 304px, 작업판은 남은 공간을 사용한다.

디자인 토큰 초안:

| 역할 | 제안 |
| --- | --- |
| 작업판 | `#FFFFFF` |
| 작업 공간 | `#ECEEF0` |
| 주 텍스트 | `#000000` |
| 보조 텍스트 | `#61666B` |
| 구분선 | `#D7DBDF` |
| 기본 그리드·선택 강조 | `#E84535` |
| 서체 | 샘플 기본값은 Inter, 폰트 메뉴에서 지정한 12종 선택. 조작 UI는 Inter와 Pretendard를 사용하며 작업판의 폰트 변경과 분리 |
| 기본 타이포 | 본문 16px / 행간 24px / 자간 0px. 제목·보조 글은 연결된 크기 체계 |
| 형태 | 수치와 문단을 중심으로 절제된 패널, 얇은 경계, 명확한 포커스, 최소한의 동작 전환 |

책의 붉은 가이드와 활자 대비를 시각적 근거로 사용하되, 조작부의 편의성과 화면 가독성을 우선한다. 동작의 중심은 수치 변화에 따른 실시간 재배치다.

## 5. 내보내기

| 형식 | 내용과 용도 |
| --- | --- |
| SVG | 그리드만 / 텍스트만 / 둘 다 선택. 작업판 크기와 색상·불투명도 보존. 인쇄 규격은 mm 실치수 포함. Figma 등에서 가져올 수 있는 벡터 결과 |
| HTML/CSS | 텍스트·그리드·타이포그래피·반응형 규칙, 인쇄 모드의 용지 크기 스타일, 선택한 폰트·굵기의 파일과 해당 라이선스/출처 고지를 포함한 내려받기 묶음 |
| JSON | schemaVersion, 프리셋 ID·규격 체계·원래 단위·원치수·방향, 그리드 수치, 타이포그래피, 색상, 구성 식별자/seed 등. 이 앱에서 재편집 가능 |

SVG는 그리드 선과 텍스트를 담는 교환 형식이며, Figma의 네이티브 Layout Guide 설정을 생성하는 전용 플러그인과는 별개다. SVG의 텍스트 편집성·폰트 의존성을 안내하고, 내보낸 결과와 앱 화면의 줄바꿈 일치를 확인한다.

미리보기와 내보내기는 같은 레이아웃 데이터를 사용한다. 폰트 로딩과 배치 계산이 끝난 결과만 내려받는다. 사용자가 Figma와 웹 개발 모두를 선택했으므로 SVG·HTML/CSS·설정 JSON 세 형식을 첫 버전의 내보내기 범위로 확정한다.

## 6. 기술 구성과 확장

- React + TypeScript + Vite로 만든 정적 웹앱. 별도의 서버·사용자 계정·유료 API 없이 브라우저에서 계산한다.
- 그리드 계산, 문장 공급, 텍스트 측정, 배치, 내보내기 모듈을 분리한다.
- UI 컴포넌트와 출력 레이아웃을 분리하여 수치·배치 규칙을 독립적으로 검증한다.
- 지정된 12종을 폰트 카탈로그로 관리하고, 배포 파일과 라이선스/출처 고지를 함께 관리한다. 기본 Inter와 UI에 필요한 자원부터 로드하고, 나머지 서체·굵기는 선택할 때 로드한다. 폰트 목록과 메트릭 구조를 분리하여 이후 추가도 지원한다.
- localStorage에는 버전을 포함한 설정만 저장한다. 손상되거나 이전 버전인 설정에서 안전하게 복구한다.
- 첫 버전의 핵심은 지정된 서체를 이용한 자동 배치와 내보내기다. 임의의 자유 드래그 편집, 이미지 업로드, 공동 편집, Figma 전용 플러그인, 사용자 폰트 업로드는 후속 버전 후보로 둔다.

## 7. GitHub와 Cloudflare Pages

승인 후 새 프로젝트 디렉터리에서만 Git을 초기화하고 의미 있는 단위로 커밋한다. 기존 프로젝트나 전역 Git 설정은 수정하지 않는다.

저장소: `dostevskii/grid-system` (Private). GitHub CLI 계정 `dostevskii`로 생성하고 비공개 상태를 확인했다. Pages 프로젝트 `grid-system`을 생성했으며 공개 주소는 https://grid-system.pages.dev/ 이다.

GitHub 비공개 저장소와 Cloudflare Pages의 Git integration을 연결한다. `main` 푸시를 자동 빌드·배포에 연결하며, 설정은 다음과 같다.

```text
Platform: Cloudflare Pages
Production branch: main
Build command: npm run build
Build output: dist
Initial domain: Cloudflare가 발급한 *.pages.dev
```

Node와 의존성 버전은 구현 때 호환성을 확인하고 프로젝트에 고정한다. 현재 로컬 Node는 v22.15.0이다. 필요한 경우 프로젝트용 런타임을 사용하며 기존 전역 환경을 일괄 변경하지 않는다.

GitHub 저장소는 비공개로 유지하고, 디자이너들이 사용할 앱은 공개 Pages 사이트로 배포하는 구성을 제안한다. 공개 앱에 포함한 샘플 텍스트와 폰트는 방문자의 브라우저에 전달된다. 제공된 책 PDF는 로컬 참고 자료로만 사용하고 소스 저장소나 배포 파일에 넣지 않는다.

Cloudflare API Token과 Account ID 환경 변수로 Git-integrated 프로젝트 생성과 첫 프로덕션 배포를 완료했다. 최초 Git installation 오류는 사용자 로그인·저장소 한정 앱 승인·재연결 후 해소됐다. 토큰 값은 기록하거나 GitHub에 전달하지 않았으며 다른 저장소 권한은 넓히지 않았다. Direct Upload로 전환하지 않고 승인된 Git integration과 `main` 자동 배포를 유지했다.

## 8. 구현 순서와 완료 기준

1. 계획 승인과 선호 반영: 확정된 제품명 Grid System으로 프로젝트 구조·Git 초기화.
2. 작동하는 첫 화면: 20·32분할, 인쇄·웹 규격 프리셋, 단위와 방향, 수치 입력, 그리드 계산, Inter 기본값과 원문 샘플.
3. 자동 배치: 12종 폰트 선택과 로딩, 서체별 지원 굵기, 세 가지 구성, 자간·행간·색상·불투명도, 보기 방식, 크기 변경 대응.
4. 재사용: SVG·HTML/CSS·JSON 내보내기와 JSON 불러오기, 설정 복원.
5. 검증과 마감: 필요한 자동 테스트, 실제 브라우저 조작, 시각 검토 및 수정.
6. 전달: 로컬 커밋, GitHub 비공개 저장소 생성·push, Cloudflare Pages 연동·배포, 배포 URL에서 최종 확인.

완료 기준:

- 최초 실행 시 4 × 5 = 20분할이 표시되고, 32분할 선택 시 4 × 8로 전환된다.
- 모든 인쇄 프리셋의 치수를 출처 표와 대조하고, ISO/JIS B판 및 국내 국절/완성 판형을 구분한다.
- 세로·가로 전환과 mm/inch/px/pt 변환에서 원래 치수를 보존한다. A4 SVG의 물리적 크기는 정확히 210 × 297 mm다.
- 웹 프리셋의 명칭·치수가 확인한 Figma Frame 프리셋과 일치하고, 기본 Desktop은 1440 × 1024 px다. 각 작업판의 높이는 자유롭게 변경 가능하다.
- 사용자 입력과 계산된 모듈 크기가 일치하며, 잘못된 값에 대해 구체적인 오류를 안내한다.
- 샘플 원문이 순서대로 반복되고 문단이 그리드와 읽기 순서에 맞춰 배치된다.
- 폰트 목록 12종과 Inter 기본값이 정확하며, 선택한 폰트·굵기를 로드한 뒤 실제 글자 폭으로 재배치한다. 독일어 원문의 움라우트·ß·인용부호를 확인한다.
- 빠른 서체 변경, 폰트 로딩 실패, 굵기 범위 변경에도 미리보기와 다운로드가 같은 확정된 폰트 상태를 사용한다.
- 글자 크기·자간·행간을 바꿔도 텍스트 충돌이나 잘린 행이 발생하지 않는다. 공간 부족은 안내된다.
- 그리드와 글자 색상·불투명도가 독립적으로 적용된다.
- 같은 저장 설정으로 같은 배치를 복원하고, 다운로드 파일을 실제로 열어 확인한다.
- 데스크톱과 좁은 화면에서 설정 조작·키보드 접근·미리보기가 가능하다.
- 타입 검사, 그리드 산식·입력 경계·배치 불변조건·JSON 왕복의 핵심 테스트, 프로덕션 빌드가 통과한다.
- 실제 배포 페이지에서 폰트와 다운로드가 동작하며 브라우저 오류가 없다.
- 저장소의 Private 상태와 배포 커밋을 확인하고 URL을 전달한다.

## 9. 참고 자료

- 사용자 제공 도서: `C:\Users\JohnHB\Downloads\Grid systems in graphic design - Josef Muller-Brockmann.pdf`
- [Cloudflare Pages Git integration](https://developers.cloudflare.com/pages/get-started/git-integration/): 비공개 GitHub 저장소 지원과 자동 빌드·배포.
- [Cloudflare Pages React guide](https://developers.cloudflare.com/pages/framework-guides/deploy-a-react-site/): `main`, `npm run build`, `dist` 설정.
- [Cloudflare Pages GitHub integration](https://developers.cloudflare.com/pages/configuration/git-integration/github-integration/): GitHub App 연동.
- [Inter 공식 사이트](https://rsms.me/inter/): 서체와 배포 자료.
- 인쇄 규격·단위·웹 작업판 출처: [PRESETS.md](./PRESETS.md).
- 사용자 추가 폰트 목록과 배포 출처: [FONTS.md](./FONTS.md).

## 승인 기록

이 계획은 Grid System의 인쇄·웹 프리셋을 포함한 위 기능을 구현·검증하고, 비공개 GitHub 저장소로 push한 뒤 공개 Cloudflare Pages 앱으로 배포하는 범위다. 사용자가 내보내기 범위를 확정한 뒤 “진행해.”라고 답하여 전체 구현을 승인했다. 아래 세부 문단은 승인 당시의 설계 의도를 보존하며, 실제 상태와 한계는 구현 문서에 기록한다.
