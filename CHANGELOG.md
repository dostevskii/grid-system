# 변경 기록

## 1.1.1 — 2026-09-16

- 글자 크기 입력을 pt 전용으로 변경하고 px/pt 선택기를 제거했다. 기본 12pt, 범위 4.5~90pt이며 0.25pt 단위로 조절한다.
- 행간·자간·그리드 치수의 px 입력은 유지한다. 폰트 크기 오류 안내도 영문·한국어 모두 pt 기준으로 표시한다.
- 기존 JSON, 브라우저 저장값, 랜덤 시드의 실제 크기를 유지한다. 입력한 pt를 내부 CSS px로 변환하므로 18pt는 기존과 같은 24px 크기로 미리보기·내보내기한다.

## 1.1.0 — 2026-09-16

- 행간을 1~160px로 확장했다. 실제 글리프 높이보다 작은 행간도 경고와 함께 렌더링하며 SVG·HTML/CSS·JSON에 보존한다. 제목 행간의 강제 최소값도 제거했다.
- 상단에 Random layout과 Seed / Apply seed를 추가했다. 같은 작업판·서체·시드는 컬럼, 행, 네 방향 여백, 가로·세로 거터, 글자 크기·굵기, 자간, 행간, 문단 채우기 양을 동일하게 생성한다.
- 새로운 자유 배치는 템플릿 선택이 아니라 그리드 전체의 시작 셀과 영역 크기를 시드로 생성한다. 영역 간 충돌을 피하면서 제목·본문을 중간·하단에 놓고 의도적인 빈 공간을 남긴다.
- UI 기본 언어를 English로 변경하고 한국어 선택과 브라우저 내 언어 저장을 추가했다. 샘플 독일어 텍스트는 변경하지 않았다.
- 큰 작업판의 과도한 텍스트 생성을 전체 10,000행으로 제한하고 경고한다.
- 기존 schemaVersion 1 설정 JSON을 계속 읽을 수 있다. 새 자유 배치는 `layout: "free"`로 저장한다.

## 1.0.0 — 2026-09-16

- 20·32분할, 60개 웹/인쇄 프리셋, 12종 서체, 자동 문단 배치, SVG·HTML/CSS·JSON 내보내기를 구현했다.
- 비공개 GitHub 저장소와 Cloudflare Pages Git 자동 배포를 연결했다.
